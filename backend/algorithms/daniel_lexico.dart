/**
 * Prueba de código para Daniel Cortez
 * Snippet de prueba para las reglas y tokens de Daniel Cortez
 * (double, bool, Set, operador flecha, operadores lógicos, nulabilidad, 
 * operadores relacionales, decimales y booleanos).
*/
void main() {
  double decimal = 15.5;
  bool bandera = true;
  Set<double>? miSet;
  double? valorNulable;
  
  bool resultado = (decimal == 15.5) && (decimal != 10.0) || !(decimal <= 5.0) || (decimal >= 2.0);
  
  int calcular() => 42;
}
/*Caracter(es) no reconocido lexicamente implementado para fallar a proposito */
¿¡
